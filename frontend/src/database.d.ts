declare namespace models {
  interface Models {
    id: number
    name: string
    finisingStitchFrequency?: number
    stitchFrequency?: number
    ageGroup?: string
    sizeGroup?: string
    isFolder?: boolean
    parent?: number
  }
}
